# Spring Boot representative sample

This sample is only for AST route and function extraction tests.
It is not wired into docker-compose or frontend import presets.

Coverage:
- class-level prefix + method-level mappings
- methods: GET POST PUT PATCH DELETE HEAD OPTIONS
